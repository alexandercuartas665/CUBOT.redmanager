@page "/planes"
@attribute [Authorize(Policy = AppPolicies.PlatformOperator)]

<ModulePlaceholder Eyebrow="Super Admin SaaS" Title="Planes y limites" Description="Planes comerciales con limites dinamicos y feature flags por plan." />
