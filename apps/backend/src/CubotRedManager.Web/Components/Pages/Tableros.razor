@page "/tableros"
@attribute [Microsoft.AspNetCore.Authorization.Authorize(Policy = AppPolicies.TenantMember)]

<ModulePlaceholder Eyebrow="Operacion diaria" Title="Tableros de tareas" Phase="Fase 3"
                   Description="Kanban del equipo por cliente o campania (origen del prototipo)." />
