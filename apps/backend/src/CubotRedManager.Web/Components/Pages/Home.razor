@page "/"
@attribute [Authorize]
@inject NavigationManager Nav

@code {
    // La raiz redirige al Dashboard (entrada de la consola de agencia).
    protected override void OnInitialized() => Nav.NavigateTo("/dashboard", replace: true);
}
